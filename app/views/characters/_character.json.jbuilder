json.extract! character, :id, :name, :age, :race, :hp, :physical_attack, :physical_defense, :physical_critical_chance, :evasion, :accuracy, :hp_recovery, :chance_block_shield, :cash, :created_at, :updated_at
json.url character_url(character, format: :json)
